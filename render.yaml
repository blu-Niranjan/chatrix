services:
  - type: web
    name: chatrix
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn mysite.wsgi:application
    workingDir: mysite
    autoDeploy: true
